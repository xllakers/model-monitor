"""
Artificial Analysis: AA API requires a paid key, so speed data is unavailable.
This module returns empty — pricing comes from openrouter.py instead.
"""

def fetch() -> dict:
    """Speed data requires AA API key. Returns empty dict."""
    return {}
